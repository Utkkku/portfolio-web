// API Route tests are better suited for E2E tests (Playwright)
// This file is kept for reference but tests are moved to e2e/admin.spec.ts
// Unit tests for auth logic are in __tests__/lib/auth.test.ts

describe('/api/admin/login - API Route Tests', () => {
  it.skip('API route tests should be run via E2E tests', () => {
    // API route tests are tested via Playwright E2E tests
    // See: e2e/admin.spec.ts
    expect(true).toBe(true)
  })
})

